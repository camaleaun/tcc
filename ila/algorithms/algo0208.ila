//prog algoritmo208
variaveis
numerico  i,ant,atual,prox, linha, col
inicio
limpar 
ant= 0 
atual=1
linha=1
col=0 
para i = 1 ate 10 
posicionar 1,  col + 1
escrever  atual
col = col + 4 
prox = ant +atual 
ant = atual 
atual = prox
proximo   
fim
