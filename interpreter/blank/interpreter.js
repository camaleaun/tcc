/*jslint devel: true*/
var JSrun = function () {
        'use strict';
    },
    init = function () {
        'use strict';
        document.getElementById('run').addEventListener('click', JSrun);
    };

document.addEventListener('DOMContentLoaded', init);
